// Upgrade scripts migrate saved action/feedback options across breaking changes.
// None yet. Once an entry is added it can NEVER be removed (Companion runs them in
// order on every config saved by an older version).
export default []
